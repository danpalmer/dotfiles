if [ -x "$(command -v stack)" ]; then
    stack setup
    stack install stylish-haskell
fi

config_file = "~/.stack/config.yml"
if [ ! -f "$config_file" ]; then
	ln -s ~/.dotfiles/haskell/stack-config.yml $config_file
fi
